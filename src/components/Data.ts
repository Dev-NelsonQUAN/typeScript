export interface data {
  bookDetails: {
    books: string,
    price: number,
    bookRead: boolean
  };
  shopDetails: {
    shopName: string
  };
  readersDetails: {
    readerName: string,
    readerAge: number
  }
}